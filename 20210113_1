#include <stdio.h>

int main(void){
unsigned char x = 255;
char y = 10;
unsigned char rez = (x + y);
printf("%d\n", rez);
return 0;
}

/*Получава се резултат 9, защото най-голямата стойност, 
която може да заема променлива от тип char (или по-точно unsigned char)
e 255 (1111 1111) и когато към нея се добавят нови стойности
тя "превърта" и започва да брои добавените стойности от 0 (0000 0000).*/
